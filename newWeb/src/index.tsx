import React from "react";
import ReactDOM from "react-dom/client";
import PortPolio from "@src/page/PortPolio";
import "./index.css";

const root = ReactDOM.createRoot(
  document.getElementById("root") as HTMLElement
);
root.render(<PortPolio />);
